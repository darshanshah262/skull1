#include<stdio.h>
#include<conio.h>
int get()
{
    int a;
    printf("enter the number\n");
    scanf("%d",&a);
    return a;
}
int logic(int a,int b, int c)
{
    if(a>c)
    {
        if(a>b)
         return a;
        if(b>a)
         return b;
    }
    
    if(c>a)
    {
        if(c>b)
         return c;
    }
   
}
int output()
{
printf();
}
void main()
{
    int a,b,c;
    a=get();
    b=get();
    c=get();
    logic(a,b,c);
}
